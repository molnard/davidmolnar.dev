// Stable NFC/QR entrypoint target.
// Encode https://davidmolnar.dev/addcontact on the production NFC tag,
// then change this value when the card should point somewhere else.
export const cardUrlTarget = '/addcontact';
